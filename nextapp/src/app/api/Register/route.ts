import User from "@/Models/UserModel";
import connectDB from "@/lib/DB/connectDB"
import { NextResponse } from "next/server";
//this is not express this is nextjs new api route

export async function POST(req:Request) {
  
}